import React from 'react';
import ReactDOM from 'react-dom/client';
import GradientComponentsDemo from './Demo';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GradientComponentsDemo />
  </React.StrictMode>,
);

